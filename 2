#!/bin/zsh

PATH=/sbin:/usr/sbin:/bin:/usr/bin

echo "Init Stage 2"

source /etc/runit/vars
cd "$basedir"
./generate-services

source ./vars
svdir=$svroot/services

exec env - PATH=$PATH runsvdir -P "$svdir" 'log: ...........................................................................................................................................................................................................................................................................................................................................................................................................'
